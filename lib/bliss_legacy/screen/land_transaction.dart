import 'package:flutter/material.dart';

class LandTransaction extends StatefulWidget {
  const LandTransaction({Key? key}) : super(key: key);

  @override
  State<LandTransaction> createState() => _LandTransactionState();
}

class _LandTransactionState extends State<LandTransaction> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(), body: Container());
  }
}
